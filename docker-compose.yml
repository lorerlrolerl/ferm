services:
  fermlog:
    build: .
    container_name: fermlog
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      # ferm.db lives directly in the fermlog folder, not in a subfolder
      - /volume1/docker/fermlog:/app/data
    env_file:
      - .env
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3